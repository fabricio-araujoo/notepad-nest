import { ExceptionGlobalFilter } from './exception-global.filter';

describe('ExceptionFilterGlobalFilter', () => {
  it('should be defined', () => {
    expect(new ExceptionGlobalFilter()).toBeDefined();
  });
});
